insertComment=INSERT INTO BOARD_COMMENT VALUES(SEQ_CNO.NEXTVAL, ?, ?, ?, DEFAULT, DEFAULT)

selectList=SELECT * FROM BOARD_COMMENT WHERE BNO = ? AND STATUS = 'N'

updateComment=UPDATE BOARD_COMMENT SET CCONTENT = ? WHERE CNO = ?

deleteComment=UPDATE BOARD_COMMENT SET STATUS = 'Y' WHERE CNO = ?